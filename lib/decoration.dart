
import 'package:flutter/material.dart';

class style_arguments extends TextStyle {
  final Color? color;
  final FontWeight fontWeight;
  final double fontSize;
  final String fontFamily;

  style_arguments(this.color, this.fontWeight, this.fontSize, this.fontFamily);
}

const Color customBackground = Color(0xffe6e6ec);
const Color customBlue = Color(0xff0e4e93);
const Color customOceanBlue = Color(0xff638181);
const Color customBlack54 = Colors.black54;
const Color customBackgroundWhite = Colors.white;


Color? primaryRed = Colors.redAccent[400];
Color? customGrey = Colors.grey[100];
Color? secondaryRed = Color(0xfffe5c5c);
Color white = Colors.white;
